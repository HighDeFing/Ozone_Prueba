{
  "name": "python-converter",
  "version": "1.0.0",
  "description": "python converter model 3d in GLB",
  "main": "converter.js",
  "scripts": {
    "start": "python __init__.py",
    "install-dependencies": "pip install pygltflib && pip install werkzeug && pip install flask && npm install obj2gltf"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "obj2gltf": "^3.1.0"
  }
}
